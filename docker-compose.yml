version: '3.8'
services:
  backend:
    image: logxapp/logx-backend:latest
    container_name: logx-backend
    restart: unless-stopped 
    ports:
      - "127.0.0.1:8080:8000"
    env_file:
      - ./backend/.env
    