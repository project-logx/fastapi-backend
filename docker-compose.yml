version: '3.8'
services:
  backend:
    image: logxapp/logx-backend:ci
    container_name: logx-backend
    restart: unless-stopped 
    ports:
      - "8080:8000"
    env_file:
      - ./backend/.env
    